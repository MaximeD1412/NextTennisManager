[package]
name = "physics-core"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.13"
tonic = "0.12"
rand = { version = "0.8", features = ["small_rng"] }

[build-dependencies]
prost-build = "0.13"
